apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: letsencrypt-pihole-cert
  namespace: default
spec:
  secretName: letsencrypt-pihole-secret
  dnsNames:
    - pi.sbb.org
  issuerRef:
    name: letsencrypt-pihole
    kind: ClusterIssuer